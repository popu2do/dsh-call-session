# dsh-call-session

DSH 进程内的会话协作与状态共享扩展，为多智能体与人类用户提供异步黑板共享、点对点单播调度与全局拓扑视图。

## 核心语言

### 协作原语

**公共黑板 / Public Blackboard**:
进程内的拉取式状态存储区，用于存放事实、任务状态与阶段产物，读取与发布绝不主动唤醒任何会话。
_Avoid_: 全局事件总线, 广播信道, 消息队列

**黑板条目 / Blackboard Post**:
发布在公共黑板上的单条结构化数据，具备主题、内容与生命周期状态。
_Avoid_: 广播消息, 事件通知

**单播调用 / Unicast Call**:
严格 1 对 1 推送给单一目标会话的调度通知，用于驱动协同推进。
_Avoid_: 群发, 广播调用, 多播

**工作区 / Workspace**:
会话运行的本地工程根目录，定义了大模型代理读取与操作文件的边界。
_Avoid_: 项目组, 租户

### 画布观测

**看板 / Canvas**:
面向人类用户的全局拓扑视图，在单一画布上横向展示所有工作区、存活会话与调用关系。
_Avoid_: 遥测大屏, 监控面板, 控制台

**工作区分组列 / Workspace Group Column**:
看板中按工作区归类并横向排列的视觉容器，每列纵向展示该工作区的会话节点。
_Avoid_: 泳道, Swimlane, Workspace Lane

**看板数据 / Canvas Data**:
看板以纯只读方式获取的会话列表、黑板条目与调用连线聚合快照。
_Avoid_: 遥测, 遥测指标, 埋点数据

**调用连线 / Call Connection**:
在看板上呈现的会话间单播调度轨迹，以纯内存方式保留有限记录。
_Avoid_: 链路追踪, 日志流

**Gutter**:
工作区分组列外侧预留的专属垂直布线通道，承载跨节点与黑板连线，避免连线穿透会话卡片。
_Avoid_: 边缘走线, 留白槽, 连线间距

**Channel Split Routing**:
看板节点连线的进出通道分配规范。跨工作区调用保持右出左进；同工作区列内单播调用执行上下行双通道分流，向下单播采用右出右进走右侧通道，向上单播采用左出左进走左侧通道，杜绝横穿列中心轴与卡片间隙。
_Avoid_: 自由连线, 端口绑定, 中心连线, 统一右出左进

**Lineage Focus**:
看板连线的按需呈现机制，常态下抑制静态连线噪声，仅在实体被悬停或选中时点亮其关联链路。
_Avoid_: 连线过滤, 拓扑高亮

**Scoped Theme**:
严格收敛在看板容器内的浅色与深色双模样式，映射 DSH 宿主主题状态且绝不污染外部。
_Avoid_: 全局样式覆盖, 主题重写

**Pulse Glow**:
实体或链路在被聚焦时产生的周期性透明度或轻微描边呼吸反馈，严禁改变图元或连线的几何坐标与尺寸。
_Avoid_: 几何放大, 缩放脉冲, 震动动效

**Flow Animation**:
用于指示在途或近时调用连线的虚线位移动画（stroke-dashoffset），在浅色模式下保持低饱和度，严禁赛博朋克重度发光。
_Avoid_: 霓虹光晕, 能量脉冲, 跑马灯

**Clamped Viewport Centering**:
定位当前会话时，在维持目标节点可见性的同时，水平与垂直轴严格受内容外接矩形与安全边距约束的平移机制，杜绝首列被推向视口中心导致左侧空白与右侧列截断。
_Avoid_: 绝对居中, 强制屏幕居中, 节点单点置中

**Macro Framing Zoom Floor**:
全局自适应取景允许的最小缩放值（0.30）。超出该下限才能完整容纳的拓扑按该值取景并对称溢出视口，以可读的会话标识优先于内容完整包含。
_Avoid_: 无下限自适应, 无限缩小, 内容强制包含

**Landed Pulse**:
视口平移到达目标实体后触发的 1.2 秒双周期非形变呼吸动效，纯通过描边透明度过渡，严禁任何几何拉伸与光晕投影变化。
_Avoid_: 缩放提示, 弹跳动画, 闪烁震动

### 会话治理

**并发运行配额 / Concurrent Running Quota**:
单工作区内同时处于 running 状态（正在 runner 中执行计算）的会话总数硬上限（5 个），防止智能体失控并发派生消耗算力，空闲 idle 会话不占用该配额。
_Avoid_: 历史会话限制, 未归档配额, 活跃会话累计

**派发模型继承 / Dispatch Model Inheritance**:
平级会话创建时，默认继承派发者最新请求头中的模型、提供商与推理强度配置，保持多智能体协同模型一致性。
_Avoid_: 全局默认模型, 固定模型配置
